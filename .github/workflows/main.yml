

name: GitHubActions
on:
  push:
    branches: [ "main" ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run : sudo apt update && apt upgrade -y
      - uses: actions/checkout@v3
        name: Check out code
      - run:  | 
              echo Artem Savchuk > index.html
              cat index.html
               
      
    
       
 
          
          
 
          
          
          
          
          
